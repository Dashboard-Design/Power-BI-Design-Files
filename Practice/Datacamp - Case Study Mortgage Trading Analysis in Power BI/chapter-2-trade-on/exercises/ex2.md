
#  Compiling market data

```
Exercise ID 1562917
```

##  Assignment 

As traders, we don't just sell at any price: we sell at any **good** price. So how do we know if a price is good or not? The best way is to compare our prices to a benchmark rate, such as other prices we could potentially get somewhere else. 

In this case, we have another option to sell these loans into **uniform mortgage-backed securities (UMBS)**. Let's start compiling some market data!

**If you have lost progress, close any open workbooks and load `2_1_bids_received_solution.pbix` from the Exercises folder on the Desktop.**

##  Hints 

When we use a **left join**, only matching data from the **right table** is merged. Since we transformed the `loan_bids` table to filter out any `loan_id` that had a `0` price, the `loan_bids` table doesn't have the same number of `loan_id`'s that the `loan_data` table has.

_If you can't find the solution, you can load the report `2_2_market_data_solution.pbix` from the Exercises folder on the Desktop and review the merges.



##  Solution 

No solution was found.


