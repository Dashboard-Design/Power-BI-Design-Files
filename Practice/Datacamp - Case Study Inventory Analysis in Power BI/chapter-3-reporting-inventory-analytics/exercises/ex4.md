
#  Control decisions

```
Exercise ID 1463018
```

##  Assignment 

Part of WarmeHands policies includes giving a second chance to items with selling potential. Your task is to find interesting information on a medium value item and identify its top buyer country.

**We recommend you to keep working in your own workbook but if you lost progress you can open `3_2_Inspecting_categories_solution.pbix` from the Exercises folder on the Desktop.**

##  Hints 

- For your first visual, you should use a **Table** with `Inventory_turnover`, `Quantity_2021`, and `Description`.
- For your revenue visual, you should use a **Clustered bar chart** displaying `Description` and the sum of `Revenue_2021`.
- For the country visual, you must use a **Map** with the `Country` and sum of `Quantity` from the `Orders` table. Do not forget to put `Country` in the **Location** box.
- To make a filter with the **Filters pane**, drag a variable, drop it in a filter slot within the **Filters** pane, and create basic filtering.

**If you can't find the solution, you can load the report `3_3_control_decisions_solution.pbix` from the Exercises folder on the Desktop.**



##  Solution 

No solution was found.


