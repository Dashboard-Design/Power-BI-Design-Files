
#  Management solutions

```
Exercise ID 1463019
```

##  Assignment 

It's time to explain the ABC classes better while creating an impact to show how much revenue good items cover. Your task is to create a dashboard containing information about revenue, COGS, and a graphical representation of how much the classes cover total revenue.

**We recommend you to keep working in your own workbook but if you lost progress you can open `3_3_control_decisions_solution.pbix` from the Exercises folder on the Desktop.**

##  Hints 

- Your formula for `Rank` should look like this:

```
Rank = RANKX(ABC,ABC[percent_revenue_2021])

```

- The table from the page `Control decisions` should have the `Description`, `Inventory_turnover`, and `Quantity_2021` displayed.
- To create a visual for revenue, you must be looking at `Revenuve_2021` and `ABC`.
- Each one of the visuals from your third step should be a **Card** visual for the count of `SKU-ID`, the sum of `Total_COGS_2021`, and the average of `Inventory_turnover`.
- Your **Stacked area chart** must have variables `Rank` in **X-axis**, average of `CP_revenue` in **Y-axis**, and `ABC_class` in **Legend**. Make sure to have `Description` and `Rank` under **Tooltips**. You can find the **Tooltips** section under the **Visualizations** pane.

**If you can't find the solution, you can load the report `3_4_management_solutions_solution.pbix` from the Exercises folder on the Desktop.**



##  Solution 

No solution was found.


