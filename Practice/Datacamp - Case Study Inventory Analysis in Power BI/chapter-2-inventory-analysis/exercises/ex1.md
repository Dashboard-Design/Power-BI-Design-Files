
#  Identifying tools and metrics

```
Exercise ID 1463012
```

##  Assignment 

We will be using some important Power BI concepts for this part of the inventory analysis.

##  Instructions 

Each card describes a step or method for your analysis, but you have to determine if what they state is **TRUE** or **FALSE**.



##  Hints 

- To filter with DAX, you must use the `FILTER()` function.
- The `SELECTCOLUMNS()` function allows you to copy any column from another table, calculated or not.
- You can use `CALCULATE(SUM(X), FILTER(X_table, X &gt;= EARLIER(X)))` to compute a cumulative increase.
- Nested `IF()` functions include functions within other functions that allow you to write multiple conditions to specify categories `IF("condition_1", "True_1", IF("Condition_2","True_2","False"))`



##  Solution 

No solution was found.


