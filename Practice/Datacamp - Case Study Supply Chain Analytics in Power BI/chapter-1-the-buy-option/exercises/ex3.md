
#  Exploring suppliers

```
Exercise ID 1541055
```

##  Assignment 

Supplier selection is an important component of the buy option. Supplier selection is a multi-faceted decision and cost is one of the most important aspects to consider. 

**If you have lost any progress, close any open reports and load `1_1_Quotes_Solution.pbix` from the Exercises folder on the desktop.**

##  Hints 

Make sure you are using `Quotes` table only.

The `Quoted Cost` is specific to the supplier, part number and the volume quoted, so make sure it is aggregating correctly. The table should include the `Supplier`, `Part_Number` and `Volume` fields.

Make sure you have created a line chart visualization with `Volume` in the **X-axis**, `Unit_Cost` in the **Y-axis** and `Supplier` in the **Legend**. 

Your `Part_Number` slicer must be set to "P9091".

**If you can't find the solution, you can load the workbook `1_2_Suppliers_Solution.pbix` from the Workbooks folder on the Desktop.**



##  Solution 

No solution was found.


