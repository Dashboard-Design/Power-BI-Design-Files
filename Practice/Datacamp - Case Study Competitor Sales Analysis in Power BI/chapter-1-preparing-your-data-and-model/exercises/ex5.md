
#  Data model performance optimization

```
Exercise ID 1448591
```

##  Assignment 

Typically, when exploring data, we load a subset of the data. However, our dataset contains data from 2017 - 2021. For our analysis, we want to review the last three years (2019 - 2021) and see how our manufacturer **Sintec** performs compared to competitors. 

Next, to improve the performance of our data model, we'll want to disable the table load of `International Sales`. 

**We do not recommend doing so, but if you lost progress you can load the solution workbook of the previous exercise `1_3_append_queries_solution.pbix` from the Exercises folder**

##  Hints 

Once the data is loaded, Power BI should automatically detect the relationship between the `Sales` and `Products` table and the `Products` and `Manufacturer` table.

If you do not see these relationships, set up a relation between Sales and Products using `ProductID` and Products and Manufacturer using `ManufacturerID`. 

**If you can't find the solution, you can load the workbook `1_4_data_model_performance_optimization_solution.pbix` from the Exercises folder on the Desktop.**



##  Solution 

No solution was found.


